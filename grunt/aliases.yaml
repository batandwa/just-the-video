default:
    - watch

setup:
    - googlefonts:setup

dist:
    - env:build
    - shell:clean-build
    - setup
    - bookmarklet
    - jshint
    - sass:dev
    - jadeUsemin:dist
    - jade:dist
    - copy:dist
    - expand-include:bookmarklet
    - shell:docker-build

build:
    - dist

bookmarklet:
    - uglify:bookmarklet
    - shell:bookmarklet

bm:
    - bookmarklet
