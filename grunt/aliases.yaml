bm:
    - bookmarklet
    
bookmarklet:
    - uglify:bookmarklet
    - shell:bookmarklet

build:
    - dist

default:
    - watch

dist:
    - env:build
    - shell:clean-build
    - setup
    - bookmarklet
    - jshint
    - sass:dev
    - jadeUsemin:dist
    - jade:dist
    - copy:dist
    - expand-include:bookmarklet
    - shell:docker-build

serve:
    - concurrent:dev

setup:
    - googlefonts:setup
